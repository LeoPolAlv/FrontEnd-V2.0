import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'FrontEnd_V2.0',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
